allprojects {
  repositories {
    maven {
      url "file://${rootDir.absolutePath}/../../../../build/localMaven"
    }
    mavenCentral()
    maven { url "https://dl.bintray.com/kotlin/kotlin-eap" }
    google()
  }
}
